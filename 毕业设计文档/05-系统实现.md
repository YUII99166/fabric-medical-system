# 第五章 系统实现

## 5.1 开发环境

### 5.1.1 开发工具

| 工具 | 版本 | 用途 |
|------|------|------|
| Go | 1.18+ | 后端开发 |
| Node.js | 14.x | 前端开发 |
| Docker | 20.10+ | 容器化部署 |
| MySQL | 5.7+ | 数据库 |
| Git | 2.x | 版本控制 |
| GoLand | 2023.x | Go IDE |
| VS Code | 1.x | 前端开发 |

### 5.1.2 运行环境

**服务器配置**：
- 操作系统：Ubuntu 20.04 LTS
- CPU：4核
- 内存：8GB
- 存储：100GB SSD
- 网络：公网 IP

**区块链网络**：
- Hyperledger Fabric 1.4.12
- Docker Compose 编排
- 4个组织，8个 Peer 节点
- 1个 Orderer 节点

## 5.2 后端实现

### 5.2.1 项目结构

```
application/server/
├── api/v2/              // API 接口层
│   ├── account.go       // 用户管理
│   ├── prescription.go  // 病历管理
│   ├── access.go        // 授权管理
│   └── patient.go       // 患者功能
├── blockchain/          // 区块链集成
│   ├── sdk.go          // Fabric SDK
│   └── channel.go      // 通道操作
├── db/                  // 数据库操作
│   ├── user.go         // 用户表操作
│   └── config.yaml     // 数据库配置
├── model/               // 数据模型
│   └── model.go        // 请求/响应模型
├── pkg/                 // 工具包
│   └── app/            // 响应封装
├── router/              // 路由配置
│   └── router.go       // 路由注册
├── config.yaml          // 应用配置
└── main.go             // 程序入口
```

### 5.2.2 用户注册实现

**核心代码**（application/server/api/v2/account.go）：

```go
func Register(c *gin.Context) {
    appG := app.Gin{C: c}
    body := new(model.RegisterBody)

    // 解析请求参数
    if err := c.ShouldBind(body); err != nil {
        appG.Response(http.StatusBadRequest, "失败", 
            fmt.Sprintf("参数解析错误: %s", err.Error()))
        return
    }

    // 参数验证
    if body.AccountName == "" || body.Username == "" || 
       body.Password == "" || body.Role == "" {
        appG.Response(http.StatusBadRequest, "失败", 
            "参数存在空值，请填写完整信息")
        return
    }

    // 检查用户名是否已存在
    existingUser, _ := db.GetUserByUsername(body.Username)
    if existingUser != nil {
        appG.Response(http.StatusBadRequest, "失败", 
            "用户名已存在，请更换用户名")
        return
    }

    // 密码加密
    hashedPassword := db.HashPassword(body.Password)

    // 调用智能合约注册到区块链
    var bodyBytes [][]byte
    bodyBytes = append(bodyBytes, []byte(body.AccountName))
    bodyBytes = append(bodyBytes, []byte(body.Username))
    bodyBytes = append(bodyBytes, []byte(hashedPassword))
    bodyBytes = append(bodyBytes, []byte(body.Role))
    
    // 根据角色传递不同参数
    if body.Role == "病人" || body.Role == "药店" {
        // 病人/药店：6个参数
        bodyBytes = append(bodyBytes, []byte(fmt.Sprintf("%d", body.Age)))
        bodyBytes = append(bodyBytes, []byte(body.Gender))
    } else {
        // 医生/管理员：10个参数
        bodyBytes = append(bodyBytes, []byte(body.Organization))
        bodyBytes = append(bodyBytes, []byte(body.OrganizationName))
        bodyBytes = append(bodyBytes, []byte(body.Department))
        bodyBytes = append(bodyBytes, []byte(body.DoctorTitle))
        bodyBytes = append(bodyBytes, []byte(fmt.Sprintf("%d", body.Age)))
        bodyBytes = append(bodyBytes, []byte(body.Gender))
    }
    
    // 执行区块链交易
    resp, err := bc.ChannelExecute("register", bodyBytes)
    if err != nil {
        appG.Response(http.StatusInternalServerError, "失败", 
            fmt.Sprintf("区块链注册失败: %v", err))
        return
    }

    // 解析区块链响应
    var data map[string]interface{}
    if err = json.Unmarshal(bytes.NewBuffer(resp.Payload).Bytes(), &data); err != nil {
        appG.Response(http.StatusInternalServerError, "失败", 
            fmt.Sprintf("解析区块链响应失败: %v", err))
        return
    }

    // 同步到数据库
    err = db.RegisterUser(body.Username, body.Password, body.AccountName, 
        body.Role, body.Organization, body.OrganizationName, 
        body.Department, body.DoctorTitle)
    if err != nil {
        fmt.Printf("警告: 数据库同步失败: %v\n", err)
    }

    appG.Response(http.StatusOK, "成功", data)
}
```

**实现要点**：
1. 参数验证：检查必填字段
2. 用户名唯一性检查
3. 密码加密存储（bcrypt）
4. 根据角色传递不同参数（6个或10个）
5. 区块链交易执行
6. 数据库同步

### 5.2.3 用户登录实现

```go
func LoginWithPassword(c *gin.Context) {
    appG := app.Gin{C: c}
    body := new(model.LoginBody)

    // 解析参数
    if err := c.ShouldBind(body); err != nil {
        appG.Response(http.StatusBadRequest, "失败", 
            fmt.Sprintf("参数解析错误: %s", err.Error()))
        return
    }

    // 从数据库验证用户
    user, err := db.LoginUser(body.Username, body.Password)
    if err != nil {
        if err.Error() == "用户名或密码错误" {
            appG.Response(http.StatusUnauthorized, "失败", 
                "用户名或密码错误，请检查后重试")
        } else {
            appG.Response(http.StatusInternalServerError, "失败", 
                fmt.Sprintf("登录失败: %v", err))
        }
        return
    }

    // 构建返回数据
    data := map[string]interface{}{
        "id":           user.ID,
        "username":     user.Username,
        "account_name": user.AccountName,
        "role":         user.Role,
        "created_at":   user.CreatedAt,
        "updated_at":   user.UpdatedAt,
    }

    appG.Response(http.StatusOK, "成功", data)
}
```

**实现要点**：
1. 从数据库验证用户名和密码
2. 密码哈希比对
3. 返回用户信息（不含密码）
4. 友好的错误提示

### 5.2.4 Fabric SDK 集成

**SDK 初始化**（application/server/blockchain/sdk.go）：

```go
// 初始化 Fabric SDK
func InitSDK() error {
    sdk, err := fabsdk.New(config.FromFile("config.yaml"))
    if err != nil {
        return fmt.Errorf("failed to create SDK: %v", err)
    }
    fabricSDK = sdk
    return nil
}

// 通道执行（写操作）
func ChannelExecute(fcn string, args [][]byte) (channel.Response, error) {
    channelContext := fabricSDK.ChannelContext(
        channelID, 
        fabsdk.WithUser(userName), 
        fabsdk.WithOrg(orgName),
    )
    
    channelClient, err := channel.New(channelContext)
    if err != nil {
        return channel.Response{}, err
    }

    response, err := channelClient.Execute(
        channel.Request{
            ChaincodeID: chaincodeID,
            Fcn:         fcn,
            Args:        args,
        },
        channel.WithRetry(retry.DefaultChannelOpts),
    )
    
    return response, err
}

// 通道查询（读操作）
func ChannelQuery(fcn string, args [][]byte) (channel.Response, error) {
    channelContext := fabricSDK.ChannelContext(
        channelID, 
        fabsdk.WithUser(userName), 
        fabsdk.WithOrg(orgName),
    )
    
    channelClient, err := channel.New(channelContext)
    if err != nil {
        return channel.Response{}, err
    }

    response, err := channelClient.Query(
        channel.Request{
            ChaincodeID: chaincodeID,
            Fcn:         fcn,
            Args:        args,
        },
    )
    
    return response, err
}
```

## 5.3 智能合约实现

### 5.3.1 注册合约实现

**核心代码**（chaincode/api/account.go）：

```go
func Register(stub shim.ChaincodeStubInterface, args []string) pb.Response {
    // 验证参数：支持6个参数（病人/药店）或10个参数（医生/管理员）
    if len(args) != 6 && len(args) != 10 {
        return shim.Error(fmt.Sprintf(
            "参数个数不满足，需要6个参数（病人/药店）或10个参数（医生/管理员），实际收到%d个参数", 
            len(args)))
    }
    
    accountName := args[0]       // 账号名
    username := args[1]          // 用户名
    password := args[2]          // 密码
    role := args[3]              // 角色
    
    // 默认值
    organization := ""
    organizationName := ""
    department := ""
    doctorTitle := ""
    ageStr := ""
    gender := ""
    
    // 根据参数数量解析
    if len(args) == 6 {
        // 病人/药店注册：6个参数
        ageStr = args[4]         // 年龄
        gender = args[5]         // 性别
    } else if len(args) == 10 {
        // 医生/管理员注册：10个参数
        organization = args[4]      // 组织MSPID
        organizationName = args[5]  // 组织名称
        department = args[6]        // 科室
        doctorTitle = args[7]       // 医生职称
        ageStr = args[8]            // 年龄
        gender = args[9]            // 性别
    }

    // 参数验证
    if accountName == "" || username == "" || password == "" || role == "" {
        return shim.Error("参数存在空值")
    }

    // 检查用户名是否已存在
    results, err := utils.GetStateByPartialCompositeKeys(stub, model.AccountV2Key, []string{})
    if err != nil {
        return shim.Error(fmt.Sprintf("查询账户列表失败: %s", err))
    }
    for _, v := range results {
        if v != nil {
            var account model.AccountV2
            err := json.Unmarshal(v, &account)
            if err != nil {
                return shim.Error(fmt.Sprintf("反序列化出错: %s", err))
            }
            if account.Username == username {
                return shim.Error("用户名已存在")
            }
        }
    }

    // 解析年龄
    age := 0
    if ageStr != "" {
        fmt.Sscanf(ageStr, "%d", &age)
    }

    // 创建账户对象
    newAccount := &model.AccountV2{
        AccountId:        stub.GetTxID()[:12],  // 生成区块链ID
        AccountName:      accountName,
        Username:         username,
        Password:         password,
        Role:             role,
        Organization:     organization,
        OrganizationName: organizationName,
        Department:       department,
        DoctorTitle:      doctorTitle,
        Age:              age,
        Gender:           gender,
    }
    
    // 写入账本
    if err := utils.WriteLedger(newAccount, stub, model.AccountV2Key, 
        []string{newAccount.AccountId}); err != nil {
        return shim.Error(fmt.Sprintf("%s", err))
    }
    
    // 返回成功结果
    accountByte, err := json.Marshal(newAccount)
    if err != nil {
        return shim.Error(fmt.Sprintf("序列化成功创建的信息出错: %s", err))
    }
    return shim.Success(accountByte)
}
```

**实现要点**：
1. 灵活参数支持：6个参数（病人/药店）或10个参数（医生/管理员）
2. 用户名唯一性检查
3. 使用交易ID生成区块链ID：`stub.GetTxID()[:12]`
4. 复合主键存储：`AccountV2Key + AccountId`
5. 返回完整账户信息

### 5.3.2 病历管理合约

```go
func CreatePrescription(stub shim.ChaincodeStubInterface, args []string) pb.Response {
    // 参数验证
    if len(args) < 10 {
        return shim.Error("参数个数不满足")
    }

    // 解析参数
    patientID := args[0]
    patientName := args[1]
    chiefComplaint := args[2]
    presentIllness := args[3]
    physicalExam := args[4]
    diagnosis := args[5]
    drugJSON := args[6]
    medicalAdvice := args[7]
    doctorID := args[8]
    hospitalID := args[9]

    // 解析药品列表
    var drugs []model.Drug
    if err := json.Unmarshal([]byte(drugJSON), &drugs); err != nil {
        return shim.Error(fmt.Sprintf("药品列表解析失败: %s", err))
    }

    // 生成病历编号
    prescriptionNo := fmt.Sprintf("PR%d", time.Now().Unix())

    // 创建病历对象
    prescription := &model.Prescription{
        ID:             stub.GetTxID()[:12],
        PrescriptionNo: prescriptionNo,
        Patient:        patientID,
        PatientName:    patientName,
        ChiefComplaint: chiefComplaint,
        PresentIllness: presentIllness,
        PhysicalExam:   physicalExam,
        Diagnosis:      diagnosis,
        Drug:           drugs,
        MedicalAdvice:  medicalAdvice,
        Doctor:         doctorID,
        Hospital:       hospitalID,
        Created:        time.Now().Format("2006-01-02 15:04:05"),
        TxID:           stub.GetTxID(),
    }

    // 写入账本
    if err := utils.WriteLedger(prescription, stub, model.PrescriptionKey, 
        []string{prescription.ID}); err != nil {
        return shim.Error(fmt.Sprintf("%s", err))
    }

    // 返回结果
    prescriptionByte, err := json.Marshal(prescription)
    if err != nil {
        return shim.Error(fmt.Sprintf("序列化出错: %s", err))
    }
    return shim.Success(prescriptionByte)
}
```

## 5.4 前端实现

### 5.4.1 项目结构

```
application/web/
├── src/
│   ├── api/              // API 接口
│   │   ├── account.js    // 用户接口
│   │   └── prescription.js // 病历接口
│   ├── components/       // 公共组件
│   │   └── charts/       // 图表组件
│   ├── router/           // 路由配置
│   │   └── index.js      // 路由定义
│   ├── store/            // Vuex 状态管理
│   │   └── modules/      // 模块
│   ├── utils/            // 工具函数
│   │   └── request.js    // HTTP 请求封装
│   ├── views/            // 页面组件
│   │   ├── login/        // 登录注册
│   │   ├── account/      // 用户管理
│   │   ├── prescription/ // 病历管理
│   │   └── patient/      // 患者功能
│   ├── App.vue           // 根组件
│   └── main.js           // 入口文件
├── public/               // 静态资源
└── vue.config.js         // Vue 配置
```

### 5.4.2 用户注册页面

**核心代码**（application/web/src/views/login/index.vue）：

```vue
<template>
  <div class="register-form">
    <el-form :model="registerForm" :rules="registerRules" ref="registerForm">
      <el-form-item prop="username">
        <el-input v-model="registerForm.username" placeholder="用户名" />
      </el-form-item>
      
      <el-form-item prop="password">
        <el-input type="password" v-model="registerForm.password" 
                  placeholder="密码" />
      </el-form-item>
      
      <el-form-item prop="accountName">
        <el-input v-model="registerForm.accountName" placeholder="姓名" />
      </el-form-item>
      
      <el-form-item prop="role">
        <el-select v-model="registerForm.role" placeholder="选择角色">
          <el-option label="医生" value="医生" />
          <el-option label="病人" value="病人" />
          <el-option label="药店" value="药店" />
        </el-select>
      </el-form-item>
      
      <!-- 医生角色显示组织选择 -->
      <el-form-item v-if="registerForm.role === '医生'" prop="organization">
        <el-select v-model="registerForm.organization" placeholder="选择医院">
          <el-option label="协和医院" value="TaobaoMSP" />
          <el-option label="301医院" value="JDMSP" />
          <el-option label="温江社区" value="WenjinMSP" />
        </el-select>
      </el-form-item>
      
      <!-- 病人角色显示年龄性别 -->
      <el-form-item v-if="registerForm.role === '病人'" prop="age">
        <el-input v-model.number="registerForm.age" placeholder="年龄" />
      </el-form-item>
      
      <el-form-item v-if="registerForm.role === '病人'" prop="gender">
        <el-select v-model="registerForm.gender" placeholder="性别">
          <el-option label="男" value="男" />
          <el-option label="女" value="女" />
        </el-select>
      </el-form-item>
      
      <el-button type="primary" @click="handleRegister">注册</el-button>
    </el-form>
  </div>
</template>

<script>
import { register } from '@/api/account'

export default {
  data() {
    return {
      registerForm: {
        username: '',
        password: '',
        accountName: '',
        role: '',
        organization: '',
        organizationName: '',
        age: 0,
        gender: ''
      },
      registerRules: {
        username: [
          { required: true, message: '请输入用户名', trigger: 'blur' }
        ],
        password: [
          { required: true, message: '请输入密码', trigger: 'blur' },
          { min: 6, message: '密码至少6位', trigger: 'blur' }
        ],
        accountName: [
          { required: true, message: '请输入姓名', trigger: 'blur' }
        ],
        role: [
          { required: true, message: '请选择角色', trigger: 'change' }
        ]
      }
    }
  },
  methods: {
    handleRegister() {
      this.$refs.registerForm.validate(async (valid) => {
        if (valid) {
          try {
            // 根据组织设置组织名称
            const orgMap = {
              'TaobaoMSP': '协和医院',
              'JDMSP': '301医院',
              'WenjinMSP': '温江社区'
            }
            this.registerForm.organizationName = 
              orgMap[this.registerForm.organization] || ''

            // 调用注册接口
            const res = await register(this.registerForm)
            if (res.code === 200) {
              this.$message.success('注册成功')
              this.$router.push('/login')
            } else {
              this.$message.error(res.msg || '注册失败')
            }
          } catch (error) {
            this.$message.error('注册失败: ' + error.message)
          }
        }
      })
    }
  }
}
</script>
```

**实现要点**：
1. 表单验证：必填项、密码长度
2. 动态表单：根据角色显示不同字段
3. 组织映射：MSPID 到组织名称
4. 错误处理：友好的错误提示
5. 成功跳转：注册成功后跳转登录页

### 5.4.3 病历列表页面

```vue
<template>
  <div class="prescription-list">
    <el-table :data="prescriptionList" border>
      <el-table-column prop="prescriptionNo" label="病历编号" />
      <el-table-column prop="patientName" label="患者姓名" />
      <el-table-column prop="diagnosis" label="诊断结果" />
      <el-table-column prop="doctorName" label="医生" />
      <el-table-column prop="hospitalName" label="医院" />
      <el-table-column prop="created" label="创建时间" />
      <el-table-column label="操作">
        <template slot-scope="scope">
          <el-button size="small" @click="viewDetail(scope.row)">
            查看详情
          </el-button>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>

<script>
import { getPrescriptionList } from '@/api/prescription'

export default {
  data() {
    return {
      prescriptionList: []
    }
  },
  mounted() {
    this.loadPrescriptions()
  },
  methods: {
    async loadPrescriptions() {
      try {
        const res = await getPrescriptionList()
        if (res.code === 200) {
          this.prescriptionList = res.data
        }
      } catch (error) {
        this.$message.error('加载病历列表失败')
      }
    },
    viewDetail(row) {
      this.$router.push({
        path: '/prescription/detail',
        query: { id: row.id }
      })
    }
  }
}
</script>
```

### 5.4.4 数据可视化实现

**ECharts 柱状图组件**（application/web/src/components/charts/BarChart.vue）：

```vue
<template>
  <div :id="chartId" :style="{ width: width, height: height }"></div>
</template>

<script>
import * as echarts from 'echarts'

export default {
  props: {
    chartId: {
      type: String,
      required: true
    },
    chartData: {
      type: Object,
      required: true
    },
    width: {
      type: String,
      default: '100%'
    },
    height: {
      type: String,
      default: '400px'
    }
  },
  watch: {
    chartData: {
      deep: true,
      handler() {
        this.renderChart()
      }
    }
  },
  mounted() {
    this.renderChart()
  },
  methods: {
    renderChart() {
      const chart = echarts.init(document.getElementById(this.chartId))
      const option = {
        title: {
          text: this.chartData.title
        },
        tooltip: {},
        xAxis: {
          data: this.chartData.xData
        },
        yAxis: {},
        series: [{
          name: this.chartData.seriesName,
          type: 'bar',
          data: this.chartData.yData,
          itemStyle: {
            color: '#409EFF'
          }
        }]
      }
      chart.setOption(option)
    }
  }
}
</script>
```

## 5.5 关键技术实现

### 5.5.1 密码加密

使用 bcrypt 算法加密密码：

```go
import "golang.org/x/crypto/bcrypt"

// 密码加密
func HashPassword(password string) string {
    bytes, _ := bcrypt.GenerateFromPassword([]byte(password), 14)
    return string(bytes)
}

// 密码验证
func CheckPasswordHash(password, hash string) bool {
    err := bcrypt.CompareHashAndPassword([]byte(hash), []byte(password))
    return err == nil
}
```

### 5.5.2 权限控制

基于角色的路由守卫：

```javascript
router.beforeEach((to, from, next) => {
  const userInfo = store.getters.userInfo
  const role = userInfo ? userInfo.role : null

  // 未登录跳转登录页
  if (!userInfo && to.path !== '/login') {
    next('/login')
    return
  }

  // 检查路由权限
  if (to.meta.roles && !to.meta.roles.includes(role)) {
    next('/403')
    return
  }

  next()
})
```

### 5.5.3 HTTP 请求封装

```javascript
import axios from 'axios'

const service = axios.create({
  baseURL: '/api/v2',
  timeout: 10000
})

// 请求拦截器
service.interceptors.request.use(
  config => {
    // 添加 token
    const token = localStorage.getItem('token')
    if (token) {
      config.headers['Authorization'] = `Bearer ${token}`
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器
service.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      Message.error(res.msg || '请求失败')
      return Promise.reject(new Error(res.msg || '请求失败'))
    }
    return res
  },
  error => {
    Message.error(error.message)
    return Promise.reject(error)
  }
)

export default service
```

## 本章小结

本章详细介绍了系统的具体实现，包括后端 API 开发、智能合约编写、前端页面实现和关键技术实现。通过代码示例展示了用户注册登录、病历管理、数据可视化等核心功能的实现细节。系统采用前后端分离架构，区块链和数据库双存储机制，实现了安全、高效的医疗信息管理。
